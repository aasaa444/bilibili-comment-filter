---
status: accepted
---

# 本机优先、云端可部署的分析服务

工具首版在用户本机运行采集与分析服务，浏览器插件通过本机接口提交 B 站视频任务；服务层保留可配置的部署边界，使同一套接口未来能够迁移到云端，而不把云端部署作为首版前置条件。这样可以先快速验证评论采集、AI 筛选和 UID 名单流程，同时避免过早绑定具体云平台。

默认提供 Windows 一键启动方式，并同时支持 Docker Compose 部署；两种方式共享同一套服务接口和持久化模型。
