# B 站评论过滤工具

本上下文描述一个面向个人 B 站账号的评论过滤工具。工具维护可由用户复核的账号候选名单，并支持本地隐藏与后续的账号拉黑操作。

## 账号识别

**B 站 UID**：B 站账号的稳定唯一标识，是候选名单、去重和后续操作的主键。

_避免_：把昵称、签名或评论文本当作账号唯一标识。

**昵称**：B 站账号当前可见的展示名称，只用于辅助识别和展示，不承担唯一标识职责。

_避免_：用昵称替代 UID 进行持久化匹配。

## 过滤与执行

**本地隐藏名单**：由 UID 组成的个人过滤集合；名单中的账号在支持的 B 站页面上，其评论和回复会被插件立即隐藏。

_避免_：把本地隐藏描述成删除评论或影响其他用户的全站状态。

**拉黑队列**：由本机服务按序自动消费的 B 站账号拉黑任务集合；用户可以暂停、恢复或重试任务，每个任务关联一个 UID 并保留失败记录。

_避免_：把拉黑队列描述成举报队列或以规避平台控制为目的的自动化。

**评论区**：B 站视频页面下的一级评论及其楼中楼回复，是本工具的主要分析来源和隐藏范围。

**弹幕**：随视频播放呈现或可按视频查询的短消息流；当前版本不采集、不分析，也不做本地隐藏。

**评论分析范围**：AI 只分析视频评论区及楼中楼回复，不采集、分析或过滤弹幕。

## 判定语言

**高置信度詹黑信号**：明确针对詹姆斯的侮辱、恶意贬损、持续嘲讽或明显敌意表达；特定詹黑词条可以提高置信度，但词条必须结合上下文和例外规则解释。

**詹黑词条**：当前已确认的典型信号包括“巴斯特”“䟋”“天龙八部”和“粘慕斯”。“巴斯特”不能按简单子串无条件命中，“曼巴斯特”是明确的友军例外。

**种子评论样本**：用户在初始阶段提供的典型评论参考集，用于帮助 AI 识别表达方式和语境边界；样本应独立于 UID 名单，并可随规则版本更新。

种子样本至少分为两类：用户确认的詹黑正例，以及正常批评、客观讨论和友军表达等非詹黑反例。

**高召回策略**：分类优先减少漏掉詹黑的情况，允许边界账号被暂时加入本地隐藏名单；明确配置的友军例外仍然优先于宽松命中规则。

高召回命中后立即加入本地隐藏名单，并同步创建拉黑队列任务；本地隐藏不等待官方拉黑任务完成。

**昵称硬规则**：当账号当前昵称命中已配置的詹黑昵称规则时，UID 直接进入高置信度结果，不等待评论内容分析。

昵称硬规则的命中依据昵称整体语义和用户提供的詹黑型昵称样本，不等同于“昵称包含詹黑”或单个字符子串匹配。

**昵称种子样本**：用户提供的典型詹黑型昵称参考集，用于让 AI 识别谐音、外号、反讽式自称等昵称语义；样本独立于评论种子样本，并可单独版本化。

昵称种子样本允许只有正例，不要求用户额外构造完整的反例集；友军例外和已知非目标昵称可以作为独立补充。

**判定证据**：支持一次 UID 判定的可回看信息，包括 UID、昵称快照、来源视频、评论链接或摘要、命中信号、模型理由、判定时间和模型版本。

**复核记录**：用户对判定证据作出的保留、撤销、加入例外或更新规则等结果，作为名单状态和后续规则调整的依据。

**显著詹黑样例**：用户在复核时主动标记的高代表性詹黑评论，作为高质量正例沉淀，用于后续 AI 提示和分类评测。

显著詹黑样例标记后立即进入后续分析，可版本化和停用，但不回溯改写已经完成的历史判定。

**视频任务**：针对一个 B 站视频执行评论采集、AI 分析和 UID 结果生成的独立工作单元；任务证据绑定来源视频。

视频任务独立运行，但任务产生的 UID 判定会更新全局本地隐藏名单，并可创建全局拉黑队列任务。

视频任务对网络超时和临时接口错误可自动重试；遇到登录失效、验证码或明确的反自动化拦截时暂停并记录原因，不通过更换身份或其他方式绕过平台控制。

模型结果分为三态：`命中`立即本地隐藏并创建拉黑队列任务；`非目标`不产生动作；`不确定`立即本地隐藏并进入复核，但不自动创建官方拉黑任务。

模型服务不可用时，昵称硬规则和已确认词条继续生效；依赖模型的新 UID 暂不判定，任务标记为部分完成并自动重试，恢复后从未分析的内容继续。

**批量评论分析**：完整采集完成后按 UID 组织评论和楼层上下文，再按模型 token 预算分批调用 AI；不为每条评论单独调用模型。短评论区可单批完成，不确定结果才进入后续小范围分析。

**样本注入策略**：默认把当前样本版本完整注入分析批次；只有样本和评论上下文超过当前模型实际上下文预算时，才改用相关样本或压缩摘要，并记录降级方式，不静默截断。

官方拉黑队列默认由本机服务自动消费；任务可持久化、暂停、恢复和重试，遇到平台异常时暂停等待处理。

**本机管理页**：由本机服务提供的管理界面，用于查看视频任务、UID 名单、判定证据、样本库、复核记录和拉黑队列；浏览器插件只承担提交任务和页面过滤。

首版网页范围是已登录的 Chrome/Edge 桌面端普通 B 站视频页：视频评论区和楼中楼；弹幕、动态、直播、番剧、移动端和 App 不在首版范围内。

**后台工作实例**：由本机服务按需启动的独立 Chromium 工作进程，使用独立配置和登录态；运行时无弹窗、不抢焦点、不捕获键鼠输入，单 Worker 处理队列，队列为空后退出。

**样本版本**：一次经过预览、去重和发布的评论样本或昵称样本集合；后续 AI 分析引用明确版本，历史任务保留其使用的版本。

**名单权威**：后台 SQLite 保存全局 UID 名单和状态，浏览器插件只保存可离线过滤的缓存，并通过版本同步后台变更。

UID 状态包括：`hidden`、`review`、`queued`、`blocked`、`exception`、`failed` 和 `paused`；视频任务中的 `非目标` 结果不进入全局 UID 名单。

**评论上下文**：评论分析输入包括作者信息、评论正文、父评论或根评论以及同一楼层的相关回复；上下文用于补充语义，但高召回策略优先，无法确定的结果仍进入 `review` 并立即本地隐藏。

命中和不确定 UID 的判定证据保留完整评论原文、楼层上下文、来源视频和评论链接；非目标评论只进入任务统计，不作为全局证据长期展示。

**AI 分析接口**：可配置的 OpenAI 兼容模型接口，既支持外部模型，也支持本机模型；模型供应商不是领域身份的一部分。
