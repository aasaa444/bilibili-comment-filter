{
  "name": "bilibili-comment-filter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Manifest V3 extension and management UI for the local-first Bilibili comment filter",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/build-assets.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test tests/frontend/*.test.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "typescript": "^5.6.3"
  }
}
